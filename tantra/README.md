# אומנות העינוג הטנטרי — אתר סדנאות

אתר סטטי (HTML/CSS/JS ללא תלויות) בהשראת העיצוב המקורי מקנבה.

## מבנה
```
tantra/
├─ index.html      # כל תוכן האתר
├─ styles.css      # השפה העיצובית: פלטה, טיפוגרפיה, סקשנים, רספונסיביות
├─ script.js       # אנימציות כניסה, כפתור צף, טופס ההרשמה
└─ images/         # התמונות שנחתכו מהעיצוב המקורי
```

## מה צריך לעדכן

### 1. הטופס והמייל — חשוב לקרוא
הטופס שולח POST ל-FormSubmit, ששולח את הפרטים ל-`gilgulit@gmail.com`
בלי שרת משלנו. שני תנאים כדי שזה יעבוד:

1. **אירוח רגיל.** בקישור התצוגה של claude.ai יש מדיניות אבטחה (CSP)
   שחוסמת פניות לשרתים חיצוניים, ולכן השליחה שם לא תעבור. על Vercel,
   Netlify, GitHub Pages או דומיין משלכן — היא עוברת.
2. **הפעלה חד-פעמית.** בשליחה הראשונה יגיע מייל מ-FormSubmit לתיבה
   `gilgulit@gmail.com` עם קישור הפעלה. בלי לחיצה עליו — המיילים לא יישלחו.

הטופס **לא מציג הודעת הצלחה** אלא אם השרת אישר מסירה בפועל. אם השליחה
נכשלה, מוצגת הודעה כנה וכפתור שמעביר את אותם פרטים לוואטסאפ בלחיצה אחת,
כך שאף פנייה לא הולכת לאיבוד.

### 2. פרטי קשר
בראש `script.js`, באובייקט `CONTACT`:
- `WHATSAPP` — `972544799145` (054-4799145). כל כפתורי ההרשמה וכל כרטיס
  תאריך מפנים לשם.
- `EMAIL` / `FORM_ENDPOINT` — יעד הטופס.

### 3. תמונות
הקבצים ב-`images/` נחתכו מצילומי המסך של קנבה ולכן ברזולוציה נמוכה יחסית.
כדאי להחליף אותם בקבצי המקור באיכות מלאה (אותם שמות קבצים).

## פריסה
- **Vercel** — Framework Preset: `Other`, Root Directory: `tantra`, ללא build command.
- **Netlify / Cloudflare Pages** — Publish directory: `tantra`.
- כל שרת סטטי אחר: להעלות את תוכן התיקייה כמות שהוא.

## פלטת הצבעים (נדגמה מהעיצוב המקורי)
| תפקיד | קוד |
|---|---|
| זהב מרכזי | `#DDCD71` |
| קרם בהיר (פינת הגרדיאנט) | `#FFFCEA` |
| רקע העמוד | `#F9F8F0` |
| פס הסדנאות | `#D7CEB7` |
| פס סדנת הגבר | `#D9D9D9` |
| חום כותרות | `#7E5130` |
| חום כהה | `#452922` |
| טאן | `#B38A60` |
| לילך (בועות) | `#EDDBE5` |
